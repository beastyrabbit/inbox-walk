# The local Fastmail credential is injected by `pnpm dev` from Infisical `dev`.

# Non-secret runtime settings. Local development reuses ~/.pi/agent/auth.json.
CODEX_MODEL=gpt-5.6-sol
CODEX_THINKING_LEVEL=high
CODEX_INFERENCE_TIMEOUT_MS=300000
DATA_DIR=./data
TIKA_URL=http://127.0.0.1:9998
